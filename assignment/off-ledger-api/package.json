{
  "name": "off-ledger-api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "compile": "bun run tsc --noEmit src/index.ts",
    "start": "bun run src/index.ts",
    "format": "bunx --bun @biomejs/biome check --write ./src",
    "check": "bunx --bun @biomejs/biome check ./src"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
